// const contentElement = document.querySelector('.content');
// console.log(contentElement);
// contentElement.addEventListener('mousemove', (e) => {
//     const { x, y } = contentElement.getBoundingClientRect();
//     contentElement.style.setProperty("--x", e.clientX - x);
//     contentElement.style.setProperty("--y", e.clientY - y);
// });
